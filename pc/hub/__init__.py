"""Recall PC Hub — gateway, workers, and services around the memory engine.

Implements the non-memory layers of docs/system_architectures_v1.md §4:
gateway (WS hub / REST / MCP), ASR workers, proactive recall, policy engine,
device registry, supervisor, metrics, and the dashboard.
"""
